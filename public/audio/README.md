# Background music

Drop an audio file here named `ambient.mp3` and the music toggle button
(bottom-left on the home page) will start working automatically.

- Component: `src/components/BgMusic.tsx`
- To use a different filename/format, update the `SRC` constant there.
